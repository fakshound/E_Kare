/*global $, jQuery, console, window*/

var Main = (function () {
  "use strict";
  var pub = {};




        pub.setup = function () {
        };
        return pub;
        }());

        $(document).ready(Main.setup);
